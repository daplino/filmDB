{% extends 'base.html.twig' %}

{% block title %}
        <title>
            {% trans %}
                Hello
            {% endtrans %}
        </title>
    {% endblock %}
{% block body %}
<div class="container">
    <h1>
        {{"user.registration"|trans}}
    </h1>

    {{ form_start(form) }}

    {{ form_widget(form)}}
    <button type="submit" class="btn btn-success"> 
            Register</button>

    {{ form_end(form)}}
    </div>
{% endblock %}