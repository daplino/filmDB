{% extends 'base.html.twig' %}

{% block title %}
        <title>
            {% trans %}
                Hello
            {% endtrans %}
        </title>
    {% endblock %}
{% block body %}
<div class="container">
    <h1>
        {{"User.Registration"|trans}}
    </h1>

    {{ form_start(form) }}

    {{ form_widget(form)}}
    <button type="submit" class="btn btn-success"> 
            {% trans into 'fr'%}
                Add
            {% endtrans %}</button>

    {{ form_end(form)}}
    </div>
{% endblock %}